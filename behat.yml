default:
    translation:
        locale: en 

    suites:
        default:
            contexts: [ FeatureContext, WebContext]

    extensions:
        DMore\ChromeExtension\Behat\ServiceContainer\ChromeExtension: ~
        Behat\MinkExtension:
            base_url: "https://google.fr/" # Replace with your site's URL
            browser_name: chrome
            default_session: selenium2
            selenium2: 
                wd_host: 'http://192.168.20.30:4444/'
                capabilities:
                    version: ''
            javascript_session: selenium2    
        
